import { LampState } from "../playfieldLamp2";
import { RuleHandler } from "./rule-handler";
import { RuleEngine } from "./rule-engine";

export class RuleDeviceHandler implements RuleHandler {
    constructor(name: string, state: LampState){

    }
    
    handle(rule: RuleEngine) {
        
    }
}